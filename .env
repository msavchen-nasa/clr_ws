# Container user details, overrideable at BUILD TIME ONLY
# These should be set in your ~/.bashrc or environment, but for advanced users we
# give the option of using a local `.env` file.
USER_UID="${USER_UID:-1000}"
USER_UID="${USER_GID:-1000}"
